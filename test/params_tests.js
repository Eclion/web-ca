import { test, module } from 'QUnit'
// import { loadJSON, fromJSON } from '../src/model/utils'

// use window.location.protocol to determine if local of not

module('Parameter tests', () => {
  test('Correctly load json', assert => {
    // var jsonContent = loadJSON('./assets/json/profile_gol.json')
    // var params = Params.fromJSON(jsonContent)
    // assert.deepEqual(params.toJSON(), jsonContent)
  })
})
